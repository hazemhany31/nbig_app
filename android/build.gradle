buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.1.1") // عدّل حسب نسخة Gradle المطلوبة
        classpath("com.google.gms:google-services:4.3.15")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.10")
        // classpath for Flutter plugin is normally provided by Flutter tooling; اضف لو لازم
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}